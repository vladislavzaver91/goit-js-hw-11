const form = document.querySelector('#search-form');
const galleryWrapper = document.querySelector('.js-gallery');
const loadMoreBtn = document.querySelector('.js-more');
const scrollBtn = document.querySelector('.is-show');

export default {
    form,
    galleryWrapper,
    loadMoreBtn,
    scrollBtn,
};